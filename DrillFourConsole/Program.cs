using System;

class Program
{
    static void Main()
    {
        int secret = 7;
        int attemptsLeft = 3; 
        bool guessedCorrect = false;

        while (attemptsLeft > 0 || guessedCorrect == false);
        
    }
}
